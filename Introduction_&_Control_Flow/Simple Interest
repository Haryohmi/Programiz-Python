P = float(input('Enter Principal'))
R = float(input('Enter Rate'))
T = float(input('Enter Time'))

interest = P * R * T * .01
total_sum = P + interest

print(interest)

print(total_sum)

#Yes, I could use \n to create another line for total_sum and usee a single print
